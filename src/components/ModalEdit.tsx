import React, { ReactElement } from 'react';
import { useDispatch } from 'react-redux';

const ModalEdit: React.FC = (): ReactElement => {
  const dispatch = useDispatch();
  return (
    <div>
      <h1>hahaha</h1>
    </div>
  );
};

export default ModalEdit;
